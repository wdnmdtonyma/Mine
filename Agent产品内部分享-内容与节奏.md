# 从一句话，到任务完成：三年 Agent 产品实践与思考

> 核心问题：当越来越多功能被放进对话框，产品经理还需要设计什么？  
> 分享对象：产品内部同学  
> 总时长：30 分钟以内  
> 建议有效内容：约 27～28 分钟，保留 2～3 分钟机动

---

## 一、这场分享到底想讲什么

这场分享从过去三年的几段 Agent 产品实践出发，分享其中形成的一些工作经验，以及我对 Agent 产品逐渐建立起来的认知。

它首先想回答一个问题：

> **当越来越多原本分散在页面、按钮和流程里的功能，被收进一个对话框；当用户只需要说一句“我想完成什么”，产品经理还需要设计什么？**

我的核心判断是：

> **对话框只是 Agent 产品的入口。页面变简单，并不意味着产品设计变简单；原来由页面、流程和用户承担的大量工作，正在转移到系统内部。产品经理需要设计的主体，也正在从页面上的交互，转向从一句话到任务完成的整个任务执行环境。**

这里所说的“任务执行环境”，包括 Agent 如何：

- 理解用户真正想完成的目标
- 补齐执行任务所需的信息
- 选择路径并调用能力
- 在真实环境中采取行动
- 面对失败时恢复和调整
- 判断结果是否真的完成并可以交付

整场分享沿着这条问题链展开：

```text
原本分布在不同页面里的功能
        ↓
越来越多地被收进一个对话框
        ↓
用户从“寻找和操作功能”，变成“直接表达目的”
        ↓
用户只说一句话以后，谁来完成中间的工作？
        ↓
一次 Agent 任务背后究竟发生了什么？
        ↓
产品经理可以在哪些位置进行设计？
        ↓
再用真实项目中的问题、取舍和结果来验证这些判断
```

---

## 二、30 分钟节奏总览

| 时间 | 内容 | 这一段的任务 |
|---|---|---|
| 0:00–3:00 | 三年实践背景、核心问题与判断 | 从行业变化切入，提出全场主线 |
| 3:00–8:30 | 一次 Agent 任务的执行地图 | 给后续案例建立共同语言 |
| 8:30–18:00 | Aime：个人任务执行环境 | 主案例，证明“从生成到交付” |
| 18:00–26:00 | Flux：团队任务执行环境 | 把问题从个人效率推到组织效率 |
| 26:00–29:15 | 回到开场，沉淀产品判断 | 从案例上升为可迁移的方法论 |
| 29:15–30:00 | 最后一句与收尾 | 闭环，留出机动时间 |

如果现场需要包含问答，正文应进一步压缩到 25～26 分钟。

---

# 第一部分：开场

## 1. 当功能都进入对话框，产品经理还要设计什么？

**建议时间：0:00–3:00**

### 这一段的目的

- 交代今天分享来自过去三年的 Agent 产品实践与思考。
- 从大家都能感知的产品变化切入，而不是先罗列项目履历。
- 抛出“用户只说一句话以后，产品经理还要设计什么”这个核心问题。
- 给出全场判断，但暂时不预告 Aime、Flux 的演进关系。

### 建议呈现

这一页不要放个人姓名、职位，也不要做成项目目录。建议只呈现三层关系：

```text
过去：页面 / 按钮 / 表单 / 流程
                 ↓
现在：用户只需要在对话框里说一句“我想完成什么”
                 ↓
问题：原来由页面、流程和用户承担的中间工作，去了哪里？
```

在对话框下方，可以隐约展开这句话背后的任务执行环境：

- 理解意图
- 定义任务
- 组织 Context
- 拆解任务
- 选择和调用工具
- 处理权限
- 接收环境反馈
- 处理失败和重试
- 测试与验证
- 交付与反馈回流

### 建议口播

> 今天我想借过去三年做 Agent 产品的几段经历，跟大家分享一些实际的工作经验，以及我对 AI 产品逐渐形成的一点认知。
>
> 这几年有一个很明显的变化：越来越多原本分散在不同页面、按钮和流程里的功能，正在被放进一个对话框。
>
> 过去，用户要先找到正确的功能入口，理解页面和流程，再一步步完成操作。现在，用户可能不再关心功能在哪里，也不需要理解系统是怎么组织的，只需要说一句：“我想完成这件事。”
>
> 但这会带来一个很有意思的问题：当用户只需要表达目的，产品经理还需要设计什么？
>
> 因为从“说一句话”到“事情真的完成”，中间其实还有大量工作。系统需要理解目标、补齐信息、选择路径、调用工具、处理权限、面对失败，并判断最后的结果到底能不能交付。
>
> 这些工作并没有随着页面消失。它们只是从用户看得见的页面和操作流程，转移到了用户看不见的系统内部。
>
> 所以我这几年越来越明确的一个判断是：对话框只是 Agent 产品的入口，任务执行环境才是 Agent 产品真正的主体，也会成为产品经理越来越核心的设计对象。
>
> 过去，我们主要设计“用户如何一步步完成任务”；现在，我们还要设计“当用户只表达目的以后，机器如何把任务真正完成”。

### 这一段的钉子句

> **当用户不再操作功能，而只表达目的，产品经理就要开始设计目的如何被转化成结果。**

或者：

> **对话框只是入口，从一句话到任务完成的整个执行环境，才是 Agent 产品的主体。**

### 进入下一部分的转场

> 为了把这个判断说清楚，在正式讲项目之前，我们先把这句用户输入往下展开。
>
> 从用户说出一句话，到 Agent 最后交付结果，这中间到底发生了什么？系统经历了哪些节点？产品又可以在哪些位置进行设计？

---

# 第二部分：建立一张理解 Agent 产品的地图

## 2. 用户只输入一句话以后，到底发生了什么？

**建议时间：3:00–6:00**

### 从开场进入这一段的转场

> 当用户只输入一句话，到 Agent 最后交付一个结果，这中间到底发生了什么？
>
> 它拿到了哪些信息？做了哪些判断？调用了哪些工具？失败以后怎么处理？又是在什么时候决定任务已经完成的？
>
> 所以，在进入具体项目之前，我想先用几分钟，和大家对齐一张 Agent 的任务执行地图。
>
> 这张图不是为了介绍某一种技术框架，而是为了帮助我们理解：Agent 的效果是由哪些环节共同决定的；当效果不好时，我们可以从哪些地方介入。

### 一次 Agent 任务的基本过程

```text
用户表达需求
     ↓
任务定义
目标 / 边界 / 成功标准
     ↓
Context 组装
指令 / 历史 / 知识 / 当前环境
     ↓
模型判断下一步
     ↓
选择行动
回答 / 追问 / 调用工具 / 请求确认
     ↓
工具或环境返回结果
     ↓
更新 Context，重新判断
     ↺
满足结束条件
     ↓
组织并交付最终结果
     ↓
评估 / 反馈 / 数据回流
```

### 对 Agent Loop 最朴素的解释

> Agent 并不是把用户的问题交给模型，然后一次性生成答案。
>
> 它更像是在一个循环里不断地观察、判断、行动，再根据环境反馈决定下一步，直到系统认为任务已经完成。

可以记成：

```text
观察 → 判断 → 行动 → 再观察
```

### 这一段要强调的三个事实

#### 事实一：Agent 不等于一次调用模型

一次完整任务，通常包含多次模型调用。

例如：

1. 第一次判断要读取文件。
2. 工具读取文件，把结果返回。
3. 第二次模型看到文件内容，决定修改。
4. 工具执行修改，把结果返回。
5. 第三次模型决定测试或总结。

#### 事实二：失败是反馈通道，不只是异常

工具失败、测试失败、权限被拒绝，都应该以清晰的结果重新进入 Context。

好的失败信息可以帮助 Agent 修改下一步；模糊的“出错了”会直接破坏整个恢复能力。

#### 事实三：模型说“做完了”，不等于任务真的完成

任务是否可以结束，还要取决于：

- 测试是否通过
- 产物是否生成
- 是否满足成功标准
- 是否触发风险规则
- 是否需要用户确认
- 是否达到停止条件或硬上限

---

## 3. 产品经理可以设计的，不只是 Prompt

**建议时间：6:00–8:30**

### 这一段的目的

把 Agent Loop 转成产品经理能够使用的分析框架。

后面的项目案例，都可以落在这五个控制面上。

### 五个产品问题

#### 1. 它到底要完成什么？——任务定义

对应内容：

- 目标
- 边界
- 成功标准
- 角色和职责
- 输出要求
- 不能触碰的红线

产品问题：

> 我们有没有把一个模糊的用户需求，转成 Agent 可以执行，并且可以判断完成的任务？

#### 2. 它做决定时能看到什么？——Context

对应内容：

- System Prompt
- 用户当前输入
- 历史对话
- 用户记忆
- 业务知识
- 仓库和环境信息
- 工具之前返回的结果
- 规则和示例

核心判断：

> Context 不是越多越好。我们真正设计的是：在什么时机，让 Agent 看到什么信息。

#### 3. 它被允许做什么？——Action Space

对应内容：

- 可以调用哪些工具
- 可以读取什么数据
- 可以修改什么内容
- 拥有什么权限
- 哪些动作自动执行
- 哪些动作需要用户确认
- 哪些动作永远拒绝

这一层本质上是在设计 Agent 的工作环境。

#### 4. 它如何持续执行？——Loop 与 Orchestration

对应内容：

- 什么时候继续
- 什么时候重试
- 什么时候换一种方案
- 什么时候并行
- 什么时候追问用户
- 什么时候请求人工介入
- 什么时候停止

其中存在一个持续的产品取舍：

> 自由度越高，可能越灵活，但也越不稳定；流程约束越强，可能越可靠，但泛化能力会下降。

#### 5. 我们怎么知道它做得好不好？——Feedback

对应内容：

- 什么叫任务成功
- 如何判断结果正确
- 如何记录失败发生在哪一步
- 用户反馈如何回流
- 线上 Bad Case 如何转成评测集
- 下一轮应该改模型、Context、工具还是流程

核心判断：

> Agent 上线不是产品建设的结束。真正的产品能力，是把线上失败持续转化成系统下一轮的改进。

### “可控与不可控”更准确的表达

不应该简单划分成“产品可以介入”和“产品不能介入”。更准确的分类是：

#### 产品可以直接设计

- 任务定义
- Context 组装
- 工具与权限
- 执行流程
- 确认节点
- 结束条件
- 评估标准

#### 产品只能间接影响

- 模型如何理解一句话
- 模型选择什么计划
- 模型面对陌生问题时的泛化
- 每一步生成的稳定性

#### 产品只能管理、无法消除的不确定性

- 用户表达含糊
- 数据过期或缺失
- 第三方系统失败
- 环境状态变化
- 模型的概率性波动

---

# 第三部分：Aime——设计一个人的任务执行环境

## 4. Aime 的核心问题：会写代码，为什么仍然不能交付？

**建议时间：8:30–9:30**

### 进入 Aime 的转场

> 前面这张图讲的还是一个抽象的 Agent Loop。接下来，我想把它放进一个真实产品里。
>
> Aime 是我们面向字节研发群体打造的一款通用 Agent 产品，最后做到了周活 7.5 万，覆盖 90% 的研发群体。
>
> 但今天我不想重点讲它有多少用户，或者我们做了多少功能。我想讲我们当时遇到的一个更本质的问题：当模型已经能够写出质量不错的代码，为什么用户仍然不能直接把一个研发任务完整地交给它？

### 核心答案

> 后来我们发现，生成代码只是整个任务 Loop 中的一次 Action。
>
> 代码写完以后，还有环境、测试、验证、调试、失败恢复和最终交付。
>
> 模型解决的是“会不会写”，而产品需要解决的是“能不能把事情真正做完”。

因此，Aime 建设的重点不只是让模型变聪明，而是围绕模型建立一套完整的研发任务执行环境。

---

## 5. Aime 的产品视角架构

**建议时间：9:30–12:00**

### 这一段要回答的不是“系统由什么服务组成”

只回答三个产品问题：

1. Agent 做决定时能够看见什么？
2. Agent 被允许做什么？
3. 系统如何知道它真的做完了？

### 建议架构表达

```text
用户任务
主站 / IDE / 飞书 / 自动化
          ↓
任务定义与 Context 构建
规划者理解目标、拆解任务、决定下一步
          ↓
动态 Worker
探代码 / 实现 / 调研 / 写文档 / 验证
          ↓
Skills 与 MCP
Skill 教“这类事情应该怎么做”
MCP / 工具让它真正触达外部系统
          ↓
研发 Harness
云工作区 / 测试 / 验证 / 调试 / 日志
          ↓
执行结果重新进入 Context
          ↺
满足交付条件
          ↓
代码 / 文档 / 预览 / 测试结果 / 剩余风险
```

### 各层的产品含义

#### 用户任务入口

不同入口不是多个独立 Agent，而是同一套办事能力出现在用户已经工作的地方：

- 主站适合创建和观察完整任务
- IDE 贴近代码现场
- 飞书适合随时派活和获得进度
- 自动化与 OpenAPI 适合事件驱动和无人值守

#### 规划者

负责：

- 定义任务
- 组织 Context
- 拆分步骤
- 判断串行或并行
- 派发 Worker
- 追问用户
- 接收执行反馈并重新规划

#### 动态 Worker

负责真实执行：

- 探索代码
- 修改实现
- 运行命令
- 调用业务工具
- 写文档或产出预览
- 返回执行结果

#### Skills 与 MCP

- Skill：解决“这类事情应该怎么做”。
- MCP / 工具：解决“Agent 的手能否够到真实系统”。

两者不能互相替代。

#### Harness

让 Agent 从“会生成”走向“能交付”：

- 云工作区提供真实执行环境
- 测试与验证提供客观真值
- 调试与日志支持失败定位
- 权限和确认节点控制风险
- 产物与 Handoff 把任务正式交还给用户

### 不需要展开的技术细节

30 分钟的产品分享里，不展开：

- 服务部署拓扑
- 数据库与消息队列
- 模型路由细节
- MCP 协议实现
- Worker 调度算法
- 每个内部平台的具体接口
- 所有入口和产品对象清单

除非某个技术设计直接对应一个重要的产品取舍，否则都不进入正文。

---

## 6. 用一条真实任务把 Aime 跑起来

**建议时间：12:00–15:30**

### 建议选择的案例标准

最终应该使用一条自己亲历、经过脱敏的真实任务。这个任务最好满足：

- 跨越至少三个系统或工具
- 中间真实失败过一次
- Agent 根据失败发生了重新规划
- 最终结果可以被客观验证
- 能够说清楚自己参与了哪个产品判断

暂时可以使用的抽象例子：

> “根据这个需求改造一个接口，并保证相关测试通过。”

### 典型任务链

#### 第一步：定义完成标准

把“改完”翻译成：

- 改动范围
- 对应 Spec
- 不能改变的行为
- 必须通过的检查
- 最终应交付的产物

#### 第二步：组装 Context

系统拉取：

- 需求或工作项
- 相关仓库与分支
- 业务文档
- 团队规范
- 历史状态
- 相关 Skills

#### 第三步：拆解并派工

任务可能被拆成：

- 探索代码与依赖
- 形成方案
- 修改实现
- 编写或更新测试
- 执行验证

其中部分任务可以并发执行。

#### 第四步：进入真实环境执行

Worker 在云工作区中：

- 修改文件
- 安装或使用依赖
- 运行命令
- 调用研发工具链
- 获取真实环境反馈

#### 第五步：第一次测试失败

这里是整条故事的重点。

不要把失败讲成异常插曲，而要讲成：

> 测试失败产生了一条新的环境证据。这条证据被写回 Context，成为下一次规划的输入。

#### 第六步：定位、修改、重跑

规划者根据新的失败结果：

- 调整判断
- 更换方案
- 重新派发任务
- 必要时询问用户
- 触发人工接管

#### 第七步：验证通过并 Handoff

最终交付的不只是一句话“已经完成”，而是：

- 代码或其他正式产物
- 测试和验证结果
- 发生了哪些关键修改
- 还有哪些剩余风险
- 哪些内容需要人确认

### 这一段的核心结论

> **Agent 长任务体验的关键，不是它能否一次成功，而是系统能不能看见失败、理解失败，并在可控边界内恢复。**

---

## 7. Aime 的三个关键产品取舍

**建议时间：15:30–18:00**

不要按照简历逐条介绍功能，而是讲三个选择。

### 取舍一：继续优化生成，还是补齐任务闭环？

表面问题：

- 如何让模型回答得更好？
- 如何让模型写出更好的代码？

我们的判断：

> 继续提高生成质量，只是在优化 Loop 中的一个节点。生产可用的关键，是写完以后能不能被测试、验证并交付。

因此重点转向：

- 云环境
- 测试
- 验证
- 调试
- 日志分析
- 结束条件

代价：建设更重、周期更长。  
收益：成功标准从主观感受，变成客观结果。

### 取舍二：每个场景硬编码，还是建设能力生态？

我们的选择：

- 用 Skills 沉淀做事方法与专家经验
- 用 MCP 和工具连接真实系统
- 根据任务动态组装 Worker

核心价值：

> 不需要每出现一个新场景，就由核心团队重新做一套 Agent。

边界：

- Skill 解决“怎么做”
- MCP 解决“如何碰到真实系统”
- 项目 Context 解决“这次任务具体是什么情况”

### 取舍三：完全自治，还是阶段性 Handoff？

我们的选择：

- SDD：先形成方案和任务结构
- 关键节点要求用户确认
- 合适的时候本地与云端接力
- 可以并发，但不等于无限放权
- 到达风险边界时交给人

核心判断：

> 长任务不是简单地让 Agent 跑得更久，而是明确什么时候对齐、什么时候执行、什么时候确认、什么时候交还给人。

### Aime 部分的收口

> Aime 给我最大的启发是：模型能力提升，并不会自动转化成产品能力提升。
>
> 模型决定了 Agent 理论上能做什么；而 Context、工具、执行环境和验证闭环，决定了它在生产环境里究竟能交付什么。

---

# 第四部分：Flux——设计一个团队的任务执行环境

## 8. 从 Aime 到 Flux：个人提效为什么没有变成组织提效？

**建议时间：18:00–18:40**

### 转场口播

> Aime 解决了一个研发人员如何把复杂任务交给 Agent，并让它从生成代码走向最终交付。
>
> 随着 Aime 做到 7.5 万周活、覆盖绝大部分研发群体，我们原本以为，个人效率提升以后，整个组织的交付效率也会自然提升。
>
> 但后来我们观察到一个比较反直觉的现象：大家使用 Agent 越来越多，代码也写得越来越快，可需求从提出到最终上线，并没有同比例变快。

核心问题：

> **当个人使用 Agent 的效率已经很高，为什么组织交付速度没有同步提高？**

一句话定义两者的关系：

> **Aime 设计的是个人任务执行环境；Flux 设计的是团队任务执行环境。**

---

## 9. Flux 的立项洞察：瓶颈没有消失，只是发生了转移

**建议时间：18:40–21:00**

### 把完整价值链摊开

```text
产品 → 设计 → 研发 → 测试 → 调试 → 上线
```

研发环节被 Coding Agent 显著加速，但一条完整需求仍然依赖所有环节。

### 上游问题

- 产品和设计角色没有同等好用的 AI 工作方式
- 需求、设计和决策信息散落在不同系统
- 上游产物无法直接成为研发 Agent 可消费的 Context
- 同样的信息需要反复人工转述

### 下游问题

- 接口测试存在断点
- GUI 测试仍然依赖大量人工
- 测试数据和 Mock 构造不完整
- 调试与 LogID 分析链路割裂
- 发布与上线没有被 Agent 闭环

### 指标问题

- Token 消耗量证明系统被使用，但不直接证明创造了业务价值
- 周活与留存证明用户持续回来，但仍是领先指标
- 最终还需要观察需求流转时长、交付周期和业务产出

### 核心判断

> **个人提效不等于组织提效。一个局部环节变快以后，瓶颈会沿着价值链转移到其他环节。**

或者：

> **我们之前优化的是一个人的 Agent Loop，但一次真正的业务交付，本质上是一个更大的组织 Loop。**

---

## 10. Flux 的方案：把个人 Loop 扩成团队 Loop

**建议时间：21:00–23:30**

Flux 不需要再重复讲一遍 Agent 架构，而是把 Aime 的图向外扩大。

### 第一层：多个角色

- 产品
- 设计
- 研发
- 测试

每个角色既是信息生产者，也是下一阶段的 Context 提供者。

### 第二层：共享 Context

共享 Context 中包含：

- 需求与 Spec
- 设计产物
- 决策记录
- 代码变更
- 测试结果
- 任务状态
- 风险与待确认项

核心判断：

> 团队共同产物不应该只是被上传的一份附件，而应该成为下一棒 Agent 的工作现场。

### 第三层：统一调度 SOTA Agent

Flux 不重新发明一个完整 Agent，而是统一调度：

- Codex
- Claude Code
- 其他专业 Agent

平台负责：

- 统一入口
- 任务与状态感知
- Context 传递
- 多 Agent 编排
- 协作关系
- 产物与进度跟踪

### 第四层：研发后链路 Harness

- Spec Driven
- 测试与造数
- GUI / 接口验证
- 调试与 LogID 分析
- 部署与上线

### Flux 的产品定义

> Flux 不是“一个更强的聊天机器人”，而是一套让人、Agent 和组织共同工作的任务执行环境。

---

## 11. Flux 的两个关键产品决策

**建议时间：23:30–26:00**

### 决策一：为什么没有先做一个聪明的协作助理？

团队里可能会出现一个很有吸引力的方向：

- 主动催办
- 识别阻塞
- 帮用户解决问题
- 主动组织团队协作

但我们的判断是：

> 这类能力属于“没有无所谓，做不好一定会被喷”。它在预期管理上很容易成为负资产。

因此优先选择：

- 简单可靠的共享空间
- 确定的信息传递
- 清晰的任务状态
- 可解释的 Agent 调度

核心原则：

> **先做足够简单、足够有把握的事情，让用户先建立“这个产品很专业”的信任。**

### 决策二：为什么没有重新自研一个完整 Agent？

我们的选择：

> 不自研一个新的完整 Agent，而是统一编排已经成熟的 SOTA Agent。

主要判断依据：

1. 自研完整 Agent 起步更慢。
2. 用户已经形成了使用 Codex、Claude Code 等产品的习惯，迁移成本真实存在。
3. 自研能力很难长期碾压最前沿产品。
4. 真正稀缺的差异化资产，是组织协作、共享 Context 和研发基础设施集成。

因此 Flux 把资源投入到：

- 团队协作层
- 共享 Context
- 多 Agent 调度
- 研发后链路 Harness
- 内部系统深度集成

### Flux 指标如何表达

周活破万可以用于说明：

- 产品命中了真实需求
- 用户愿意持续回来
- 统一调度的入口价值成立

但不能直接说它已经证明了组织提效。

更准确的表达：

> 周活和留存是领先指标和必要条件，最终北极星仍然是需求从提出到上线的流转时长与交付效率。

### Flux 部分的收口

> Flux 让我进一步意识到，Agent 时代产品经理设计的对象还会继续扩大。
>
> 最开始，我们设计页面；后来设计一个 Agent 的任务执行环境；再后来，我们开始设计多个角色和多个 Agent，如何共享信息、传递产物，并共同完成一次业务交付。

---

# 第五部分：回到开场，沉淀自己的判断

## 12. 设计对象的三次扩张

**建议时间：26:00–27:30**

讲完 Flux 后，不需要再单独打开一个割裂的“个人思考”章节，而是直接回到开场。

### 第一阶段：设计页面

产品经理设计：

- 用户看到什么
- 用户点击什么
- 用户如何通过步骤亲自完成任务

### 第二阶段：设计个人任务执行环境

以 Aime 为代表，产品经理设计：

- Agent 如何理解目标
- 如何组织 Context
- 如何使用工具
- 如何处理失败
- 如何验证和交付

### 第三阶段：设计团队任务执行环境

以 Flux 为代表，产品经理设计：

- 多个角色如何贡献信息
- 多个 Agent 如何被调度
- 产物如何成为下一步 Context
- 人与 Agent 如何 Handoff
- 团队如何围绕共同目标完成交付

### 建议口播

> 讲到这里，我们再回到一开始的那个判断：Agent 时代，产品经理设计的对象，正在从用户看得见的页面，逐渐扩展到用户看不见的任务执行环境。
>
> Aime 让我意识到，一个界面看起来非常简单的 Agent 产品，背后需要设计任务如何被理解、Context 如何被组织、工具如何被调用、失败如何恢复，以及最终结果如何被验证。
>
> 而 Flux 又把这个问题往外推了一层：我们设计的不再只是一个 Agent 如何完成一个人的任务，还要设计多个角色、多个 Agent 和多个系统，如何共享信息、传递产物、共同完成一次业务交付。
>
> 所以 Agent 真正改变产品的地方，并不是把原来的按钮换成了一个对话框，而是把原本需要用户亲自完成的大量工作，转移到了系统内部。

---

## 13. Agent 时代，产品经理发生的三个变化

**建议时间：27:30–29:15**

### 变化一：设计单位从“功能”变成“任务”

过去容易先问：

> 这个功能有哪些页面？用户怎么操作？

现在要先问：

> 用户究竟想完成什么任务？任务的边界、成功标准和红线是什么？

任务定义发生在页面之前，也决定了后面的系统设计。

### 变化二：产品责任从“交互”扩展到“执行环境”

产品经理开始需要参与设计：

- Agent 能看到什么 Context
- 能使用哪些工具
- 拥有什么权限
- 如何规划、重试和停止
- 什么时候需要人介入
- 如何验证交付结果

这些不再只是工程细节，而是产品体验本身。

### 变化三：衡量对象从“有没有使用”走向“有没有完成”

周活、留存和渗透率仍然重要，但它们只能证明用户愿意来。

Agent 产品最终还需要回答：

- 任务成功率有没有提升
- 用户返工是否下降
- 用户接管率是否下降
- 重试是否减少
- Agent 产物是否真正进入生产
- 业务交付是否真的变快

核心判断：

> **周活证明用户愿意来，任务成功和交付效率才证明产品真正创造了价值。**

---

## 14. 最后的收尾

**建议时间：29:15–30:00**

### 建议口播

> 所以，我现在越来越觉得，Agent 时代的产品经理，不是在给模型套一个页面，而是在给任务搭建一个可执行、可验证、可协作的环境。
>
> 页面越简单，并不意味着产品越简单。恰恰相反，它意味着我们把更多复杂性从用户手里接了过来，放到了系统内部。
>
> 页面只是任务的入口，任务执行环境才是 Agent 产品真正的主体。

### 最后一页只保留一句话

> **Agent 时代，产品经理不是在给模型设计一个入口，而是在给任务设计一个能够完成它的世界。**

---

# 六、整场分享的内容边界

## 应该重点讲的

- 自己经过项目形成的判断
- 反直觉的问题
- 失败与 Bad Case
- 产品取舍与判断依据
- 为什么没有选择另一个看似合理的方案
- 产品、架构和运营如何共同构成闭环
- 哪些指标只是领先指标，哪些才是最终价值

## 不应该花时间展开的

- 完整 SOTA Agent 架构对比
- Aime 的所有入口和产品对象
- 所有 Skills、MCP 和工具清单
- 详细服务部署拓扑
- 每一个内部系统的名称和接口
- 平均介绍 Bits、Aime、Flux 三个项目
- “Agent 是未来”“模型会越来越强”等泛趋势
- 只有功能，没有冲突和判断的项目介绍

## 讲述原则

### 1. 原理是案例的地图，不是独立的技术课

Agent Loop 只讲后面会被 Aime 和 Flux 使用的概念。

### 2. 架构图只回答产品问题

只回答：

- 信息如何流动
- Agent 可以做什么
- 系统如何知道任务完成

### 3. 数据必须带边界

报数字时尽量说明：

- 指标定义
- 分母
- 时间窗口
- 它能证明什么
- 它还不能证明什么

### 4. 用“我判断了什么”体现个人价值

每个案例至少出现一次：

```text
当时存在什么分歧
        ↓
我依据什么做判断
        ↓
最终选择了什么
        ↓
付出了什么代价
        ↓
后来如何被验证或被修正
```

---

# 七、正式制作分享前仍需补齐的素材

## Aime

- [ ] 一条真实、脱敏、经历过失败恢复的任务链
- [ ] 这条任务涉及哪些系统、工具和 Context
- [ ] 第一次失败的具体原因
- [ ] Agent 如何根据失败重新规划
- [ ] 最终用什么客观标准判断成功
- [ ] 自己在其中做出的关键产品判断
- [ ] Aime 7.5 万周活的准确口径
- [ ] 90% 渗透率的准确分母
- [ ] 一个能证明“可交付”的中间指标，例如任务成功率、代码采纳率、测试通过率、用户接管率或重试率

## Flux

- [ ] 周活破万的准确活跃定义和时间窗口
- [ ] 用户持续使用或留存的准确口径
- [ ] 共享 Context 的使用或渗透数据
- [ ] 编排 SOTA Agent 后的任务量或增长数据
- [ ] “先共享区、不做协作助理”上线后的验证证据
- [ ] 需求流转或交付效率的最终指标是否已有数据
- [ ] 如果尚无最终指标，明确当前验证阶段与后续计划

## 表达与保密

- [ ] 所有项目名、内部平台名和数字是否允许在本次内部范围出现
- [ ] 真实任务是否完成脱敏
- [ ] 是否需要将具体架构表述抽象为通用概念

---

# 八、整场分享的一句话摘要

如果听众最后只能记住一条信息，希望是：

> **Agent 产品的页面可能越来越简单，但产品本身并没有变简单。产品经理正在从设计用户操作的界面，走向设计机器完成任务、人与 Agent 共同交付的执行环境。**
